[project]
name = "example-pyproject"
version = "0.0.1a1"
license-files = ["LICENSE.TXT]
authors = [ 
    {name = "izzy Muller", email = "ipm324@my.utexas.edu"}
]
build-backend = "setuptools.build_meta"
requires = [
        "setuptools",
]